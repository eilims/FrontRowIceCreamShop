import numpy as np
from matplotlib import pyplot as plt

def stateChange(inp, state, matMap, p_e):
  
  return newState

def genMap(size = (5,5), shops = [], obstacles = [], negRewards = []):
  matMap = [[0 for i in range(size[1])] for j in range(size[0])]
  for shop in shops:
    matMap[shop[0]][shop[1]] = 

if __name__ == "__main__":
  